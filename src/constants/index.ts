import { BotType } from "../config/types";

export const DEFAULTS = {
  BOT_TYPE: BotType.LONG,
  BOT_ENABLED: true,
  BOT_BALANCE_SIZE: 25,
  BOT_POSITION_SIZE: 0.015,
  BOT_MIN_ORDER_SIZE: 100,
  BOT_SPREAD: 0.001,
  BOT_RANGE: 0.0025
};
